# 鑫速录 - 企业财务报表自动化填报与智能分析系统

## 产品需求规格说明书 (PRD)

**文档版本**：v2.0  
**编写日期**：2026年8月5日  
**文档状态**：正式发布  
**适用范围**：微信小程序端 + Vue管理后台 + Spring Boot后端

---

## 目录

- [1. 产品概述](#1-产品概述)
  - [1.1 产品定位](#11-产品定位)
  - [1.2 目标用户](#12-目标用户)
  - [1.3 核心价值](#13-核心价值)
- [2. 系统架构](#2-系统架构)
  - [2.1 技术架构](#21-技术架构)
  - [2.2 三端协同关系](#22-三端协同关系)
  - [2.3 数据流图](#23-数据流图)
- [3. 功能模块总览](#3-功能模块总览)
- [4. 微信小程序端功能需求](#4-微信小程序端功能需求)
  - [4.1 用户认证模块](#41-用户认证模块)
  - [4.2 报表采集模块（核心亮点）](#42-报表采集模块核心亮点)
  - [4.3 OCR识别进度跟踪](#43-ocr识别进度跟踪)
  - [4.4 报表建档模块](#44-报表建档模块)
  - [4.5 字段复核模块](#45-字段复核模块)
  - [4.6 三大报表展示](#46-三大报表展示)
  - [4.7 数据校验模块](#47-数据校验模块)
  - [4.8 健康度评估模块](#48-健康度评估模块)
  - [4.9 分析报告模块](#49-分析报告模块)
  - [4.10 趋势分析模块](#410-趋势分析模块)
  - [4.11 报表归档模块](#411-报表归档模块)
  - [4.12 个人中心模块](#412-个人中心模块)
- [5. Vue管理后台功能需求](#5-vue管理后台功能需求)
  - [5.1 登录认证模块](#51-登录认证模块)
  - [5.2 工作台仪表盘（A01）](#52-工作台仪表盘a01)
  - [5.3 企业与报表管理（A02）](#53-企业与报表管理a02)
  - [5.4 报表详情与OCR复核（A03）](#54-报表详情与ocr复核a03)
  - [5.5 财务分析报告（A04）](#55-财务分析报告a04)
  - [5.6 历史趋势监控（A05）](#56-历史趋势监控a05)
  - [5.7 规则配置中心（A06）](#57-规则配置中心a06)
  - [5.8 审计日志管理](#58-审计日志管理)
- [6. 后端API服务需求](#6-后端api服务需求)
  - [6.1 认证授权API](#61-认证授权api)
  - [6.2 文件上传API](#62-文件上传api)
  - [6.3 OCR识别API](#63-ocr识别api)
  - [6.4 企业管理API](#64-企业管理api)
  - [6.5 财务报表API](#65-财务报表api)
  - [6.6 指标计算API](#66-指标计算api)
  - [6.7 分析报告API](#67-分析报告api)
  - [6.8 趋势分析API](#68-趋势分析api)
  - [6.9 规则配置API](#69-规则配置api)
  - [6.10 审计日志API](#610-审计日志api)
- [7. 核心业务流程](#7-核心业务流程)
  - [7.1 报表采集与处理流程](#71-报表采集与处理流程)
  - [7.2 健康度评分计算流程](#72-健康度评分计算流程)
  - [7.3 分析报告生成流程](#73-分析报告生成流程)
- [8. 数据模型设计](#8-数据模型设计)
- [9. 非功能性需求](#9-非功能性需求)
- [10. 附录](#10-附录)

---

## 1. 产品概述

### 1.1 产品定位

**鑫速录**是一款面向中小企业和财务服务机构的企业财务报表自动化填报与智能分析平台。系统通过OCR技术自动识别财务报表数据，结合AI算法进行财务健康度评估和风险预警，帮助用户高效完成报表填报、审核和分析工作，提升财务管理效率和准确性。

**产品Slogan**：拍一下，报表自动录入 —— 让财务数据采集更智能，让分析决策更高效

### 1.2 目标用户

| 用户角色 | 使用终端 | 核心场景 | 权限范围 |
|---------|---------|---------|---------|
| **客户经理/操作员** | 微信小程序 | 外出采集、现场识别、移动办公 | 报表上传、OCR复核、查看分析 |
| **财务分析师** | Vue管理后台 | 数据分析、报告生成、趋势监控 | 报表查看、分析导出、规则查看 |
| **系统管理员** | Vue管理后台 | 企业管理、规则配置、系统运维 | 全部权限、用户管理、配置管理 |

### 1.3 核心价值

#### 效率提升
- **传统方式**：手工录入平均每份报表需 **30-45分钟**
- **鑫速录方案**：OCR自动识别仅需 **3-5分钟**
- **效率提升**：**85%以上** 的时间节省

#### 风险预警
- 实现财务风险自动识别和预警
- 五维健康度模型全面评估企业状况
- 关键指标突破阈值即时告警

#### 决策支持
- 提供专业的财务分析报告
- 多维度历史趋势可视化
- 辅助信贷审批、贷后管理等业务决策

---

## 2. 系统架构

### 2.1 技术架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户层                                   │
├──────────────────────┬──────────────────────────────────────────┤
│   微信小程序端        │           Vue 2.6 管理后台                │
│   (原生WXML/WXSS)    │     (Element UI + ECharts)                │
├──────────────────────┴──────────────────────────────────────────┤
│                      API网关层 (RESTful)                        │
├─────────────────────────────────────────────────────────────────┤
│                    Spring Boot 2.7.18 后端                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ OCR引擎   │ │ 指标计算  │ │ 规则引擎  │ │ 报告生成  │          │
│  │ PDFBox   │ │ BigDecimal│ │ 阈值配置  │ │ PDF导出  │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
├─────────────────────────────────────────────────────────────────┤
│                     H2 / MySQL 数据层                           │
│         (19张核心业务表 + 完整索引优化)                          │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 三端协同关系

```
┌─────────────────┐      HTTP/HTTPS       ┌─────────────────┐
│                 │ ◄──────────────────► │                 │
│   微信小程序    │                       │  Spring Boot    │
│   (移动采集)    │                       │  后端API服务     │
│                 │ ◄──────────────────► │                 │
└─────────────────┘                       └─────────────────┘
                                                   ▲
                                                   │
                                                   ▼
                                         ┌─────────────────┐
                                         │                 │
                                         │  Vue管理后台     │
                                         │  (PC端管理)      │
                                         │                 │
                                         └─────────────────┘
```

**职责分工**：
- **微信小程序端**：负责移动端数据采集、OCR触发、实时进度跟踪、字段复核、报表查看
- **Vue管理后台**：负责企业管理、深度分析、规则配置、审计监控、批量操作
- **Spring Boot后端**：提供统一的RESTful API服务，处理核心业务逻辑和数据持久化

### 2.3 数据流图

```
[纸质报表/PDF] → [拍照/上传] → [文件存储] → [OCR识别] → [字段提取]
                                                          ↓
[三大报表] ← [数据校验] ← [人工复核] ← [置信度检查] ← [字段映射]
     ↓
[指标计算] → [健康度评分] → [分析报告] → [PDF导出]
     ↓                                              ↓
[历史归档] ← [入库存储] ────────────────────────── [分享发送]
```

---

## 3. 功能模块总览

### 3.1 功能模块清单

| 模块编号 | 模块名称 | 所属端 | 优先级 | 功能点数量 |
|---------|---------|-------|-------|-----------|
| M01 | 用户认证 | 小程序+后台 | P0 | 6 |
| M02 | 报表采集 | 小程序 | **P0(核心)** | 8 |
| M03 | OCR识别 | 小程序+后台 | **P0(核心)** | 7 |
| M04 | 报表建档 | 小程序+后台 | P0 | 5 |
| M05 | 字段复核 | 小程序+后台 | **P0(核心)** | 6 |
| M06 | 三大报表展示 | 小程序+后台 | P0 | 3 |
| M07 | 数据校验 | 小程序+后台 | P1 | 4 |
| M08 | 健康度评估 | 小程序+后台 | **P0(核心)** | 5 |
| M09 | 分析报告 | 小程序+后台 | **P1(重点)** | 7 |
| M10 | 趋势分析 | 后台为主 | P1 | 6 |
| M11 | 报表归档 | 小程序+后台 | P1 | 5 |
| M12 | 工作台仪表盘 | 后台 | P1 | 4 |
| M13 | 企业管理 | 后台 | P0 | 6 |
| M14 | 规则配置 | 后台 | P2 | 7 |
| M15 | 审计日志 | 后台 | P2 | 4 |
| **合计** | **15个模块** | **三端** | - | **83个功能点** |

### 3.2 核心亮点功能矩阵

| 亮点功能 | 小程序端 | 管理后台 | 后端API |
|---------|:-------:|:-------:|:------:|
| 📷 OCR智能识别（拍照/PDF上传） | ✅ 核心入口 | ✅ 文件上传 | ✅ 同步/异步双模式 |
| 🔍 三重质量保障机制 | ✅ 复核界面 | ✅ 审核工作台 | ✅ 置信度+勾稽校验 |
| 📊 五维健康度评估 | ✅ 圆环展示 | ✅ 雷达图+KPI卡 | ✅ 30+指标计算 |
| 📈 历史趋势追踪 | ✅ 迷你图表 | ✅ 多维趋势面板 | ✅ 时序数据分析 |
| ⚙️ 灵活规则配置 | ❌ 仅查看 | ✅ 完整配置能力 | ✅ 动态规则引擎 |

---

## 4. 微信小程序端功能需求

### 4.1 用户认证模块

**页面路径**：`/pages/profile/profile`

#### 功能描述

提供用户登录、身份验证、会话管理功能，确保数据安全和操作可追溯。

#### 功能点清单

| 序号 | 功能点 | 描述 | 接口依赖 |
|-----|-------|------|---------|
| F01-01 | 用户登录 | 支持用户名密码登录，Token本地存储 | `POST /api/auth/login` |
| F01-02 | Token管理 | 自动刷新Token，过期提示重新登录 | `POST /api/auth/refresh-token` |
| F01-03 | 登录状态检测 | 每次进入小程序时检查登录态 | 本地Storage读取 |
| F01-04 | 用户信息展示 | 显示当前登录用户名、角色、所属机构 | `GET /api/auth/userinfo` |
| F01-05 | 安全登出 | 清除本地缓存，断开会话 | `POST /api/auth/logout` |
| F01-06 | 登录拦截 | 未登录时跳转登录页并记录目标路径 | 前端路由守卫 |

#### UI交互说明

```markdown
- 登录表单：
  - 用户名输入框（支持默认值填充）
  - 密码输入框（密文显示）
  - "记住密码"选项
  - 登录按钮（主色调 #0e8f78）

- 个人中心：
  - 用户头像区域
  - 用户名/角色信息
  - "退出登录"按钮
  - 版本信息展示
```

#### 业务规则

- **默认账号**：开发环境预置 `manager / manager123`
- **Token有效期**：2小时（可通过配置调整）
- **并发限制**：同一账号最多3个设备同时在线
- **密码安全**：传输过程使用HTTPS加密

---

### 4.2 报表采集模块（核心亮点）

**页面路径**：`/pages/index/index`

#### 功能描述

**这是鑫速录最核心的差异化功能**。客户经理可以通过手机直接完成财务报表的数据采集工作，支持两种便捷方式：
1. **拍照OCR扫描**：直接拍摄纸质报表照片
2. **PDF文件上传**：从微信聊天记录或手机文件中选择电子版报表

这一功能彻底解放了客户经理，让他们从繁琐的手工录入中解脱出来。

#### 功能点清单

| 序号 | 功能点 | 详细描述 | 技术实现 |
|-----|-------|---------|---------|
| F02-01 | 拍照OCR扫描 | 调用微信`chooseMedia`接口选择相机或相册图片 | `wx.chooseMedia({mediaType:['image']})` |
| F02-02 | PDF文件上传 | 调用`chooseMessageFile`接口选择PDF文件，限制≤30MB | `wx.chooseMessageFile({type:'file', extension:['pdf']})` |
| F02-03 | 文件大小校验 | 上传前校验文件大小，超限给出友好提示 | 前端校验 + 后端双重验证 |
| F02-04 | 文件类型校验 | 仅允许jpg/png/pdf格式，其他格式拒绝 | MIME Type检查 |
| F02-05 | 上传进度显示 | 实时显示上传进度百分比 | `wx.uploadTask.onProgressUpdate` |
| F02-06 | 断点续传支持 | 网络中断后可恢复上传（大文件场景） | 分片上传 + 断点记录 |
| F02-07 | 最近报表快捷入口 | 首页展示最近处理的5条报表记录 | 本地缓存 + 接口查询 |
| F02-08 | 未登录拦截 | 未登录时弹出引导弹窗，跳转登录页 | 登录状态守卫 |

#### UI布局结构

```html
<!-- 英雄区横幅 -->
<view class="hero-section">
  <text class="hero-badge">客户经理工作台</text>
  <text class="hero-title">拍一下，报表自动录入</text>
  <text class="hero-subtitle">支持资产负债表、利润表、现金流量表</text>
</view>

<!-- OCR扫描卡片 -->
<view class="card upload-card" bindtap="chooseImage">
  <text class="icon-text">📷</text>
  <text class="upload-title">OCR 扫描</text>
  <text class="upload-desc">拍摄财务报表或从相册选择</text>
  <button>开始扫描</button>
</view>

<!-- PDF上传卡片 -->
<view class="card upload-card" bindtap="choosePdf">
  <text class="icon-text">📄</text>
  <text class="upload-title">PDF 文件上传</text>
  <text class="upload-desc">支持多页PDF，单文件≤30MB</text>
  <button>选择文件</button>
</view>

<!-- 最近报表列表 -->
<view class="report-list">
  <!-- 展示最近5条报表记录，包含企业名、期间、健康度分数 -->
</view>
```

#### 核心代码逻辑

```javascript
// 选择图片进行OCR扫描
chooseImage() {
  if (!this.ensureLoggedIn()) return;
  wx.chooseMedia({
    count: 1,
    mediaType: ['image'],
    sourceType: ['album', 'camera'], // 支持相机拍照和相册选择
    success: (res) => {
      const tempFilePath = res.tempFiles[0].tempFilePath;
      this.uploadAndRecognize(tempFilePath, 'image'); // 上传并触发OCR
    }
  });
}

// 选择PDF文件
choosePdf() {
  if (!this.ensureLoggedIn()) return;
  wx.chooseMessageFile({
    count: 1,
    type: 'file',
    extension: ['pdf'],
    success: (res) => {
      const selectedFile = res.tempFiles[0];
      if (selectedFile.size > 30 * 1024 * 1024) {
        wx.showToast({ title: 'PDF不能超过30MB', icon: 'none' });
        return;
      }
      this.uploadAndRecognize(selectedFile.path, 'pdf');
    }
  });
}
```

#### 业务规则

- **支持的文件格式**：JPG、PNG、PDF（优先推荐PDF）
- **文件大小限制**：图片≤10MB，PDF≤30MB
- **图片质量要求**：分辨率≥1920×1080，文字清晰可辨
- **PDF要求**：带文字层的标准三表PDF效果最佳（识别准确率98%+）
- **扫描件处理**：需要配置外部OCR服务（`AI_PROVIDER=http`）

#### 性能指标

| 指标项 | 目标值 | 说明 |
|-------|-------|------|
| 图片上传速度 | ≤5秒（4G网络） | 10MB以内图片 |
| PDF上传速度 | ≤15秒（4G网络） | 30MB以内PDF |
| OCR触发响应 | ≤1秒 | 发起异步任务 |
| 页面加载时间 | ≤2秒 | 首屏渲染完成 |

---

### 4.3 OCR识别进度跟踪

**页面路径**：`/pages/progress/progress`

#### 功能描述

在发起OCR识别后，用户可以实时查看识别任务的执行进度、当前状态和预计完成时间。对于耗时较长的任务（如扫描件），支持轮询刷新和后台通知。

#### 功能点清单

| 序号 | 功能点 | 描述 | 接口依赖 |
|-----|-------|------|---------|
| F03-01 | 任务状态展示 | 显示当前任务状态（PENDING/RUNNING/COMPLETED/FAILED） | `GET /api/ocr/tasks/{taskId}` |
| F03-02 | 进度条动画 | 可视化展示识别进度（0-100%） | 前端组件实现 |
| F03-03 | 已用时长计时 | 显示任务已运行时间 | 前端定时器 |
| F03-04 | 自动轮询刷新 | 每3秒自动查询一次任务状态 | `setInterval` + 接口调用 |
| F03-05 | 识别完成跳转 | 任务完成后自动跳转到复核页面 | 状态判断 + 路由跳转 |
| F03-06 | 错误处理 | 任务失败时显示错误原因和重试按钮 | 异常捕获 |
| F03-07 | 取消任务 | 允许用户主动取消正在执行的OCR任务 | `DELETE /api/ocr/tasks/{taskId}` |

#### UI交互流程

```
┌─────────────────────────────────────────┐
│  OCR识别进行中                            │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━ 67%       │
│                                          │
│  当前阶段：正在解析利润表...               │
│  已用时：00:23                           │
│  预计剩余：约15秒                         │
│                                          │
│         [ 取消任务 ]                      │
└─────────────────────────────────────────┘
              ↓ 完成
┌─────────────────────────────────────────┐
│  ✓ OCR识别完成                           │
│                                          │
│  识别字段数：140项                        │
│  平均置信度：96.8%                       │
│  高置信度字段：132项（≥80%）             │
│  待复核字段：8项（<80%）                 │
│                                          │
│  [ 查看识别结果 ]  [ 开始复核 ]            │
└─────────────────────────────────────────┘
```

#### 技术实现要点

```javascript
// 轮询任务状态
pollTaskStatus(taskId) {
  this.pollingTimer = setInterval(async () => {
    try {
      const res = await request.get(`/ocr/tasks/${taskId}`);
      const task = res.data;
      
      this.setData({
        status: task.taskStatus,
        progress: task.progress || 0,
        elapsed: this.calculateElapsed(task.createdAt)
      });
      
      if (task.taskStatus === 'COMPLETED') {
        clearInterval(this.pollingTimer);
        wx.navigateTo({ url: `/pages/review/review?taskId=${taskId}` });
      } else if (task.taskStatus === 'FAILED') {
        clearInterval(this.pollingTimer);
        this.showError(task.errorMessage);
      }
    } catch (e) {
      console.error('轮询失败', e);
    }
  }, 3000); // 每3秒轮询一次
}
```

---

### 4.4 报表建档模块

**页面路径**：`/pages/archive/archive`

#### 功能描述

在OCR识别完成后，需要将识别结果与企业信息关联，建立完整的报表档案。此模块负责收集企业基本信息、报表期间等元数据，为后续分析和归档做准备。

#### 功能点清单

| 序号 | 功能点 | 描述 | 接口依赖 |
|-----|-------|------|---------|
| F04-01 | 企业信息录入 | 输入或选择企业名称、统一社会信用代码 | `GET/POST /api/enterprises` |
| F04-02 | 报表期间设置 | 选择报表归属期间（年/季度/月） | 前端Picker组件 |
| F04-03 | 报表类型选择 | 选择报表类型（月报/季报/年报/临时报） | 枚举值选择 |
| F04-04 | 数据来源标识 | 标记数据来源（扫描件/电子版/手工录入） | 下拉选择 |
| F04-05 | 客户经理关联 | 关联当前操作的客户经理 | 从Token获取用户信息 |

#### 表单字段定义

| 字段名 | 字段编码 | 类型 | 必填 | 校验规则 | 示例值 |
|-------|---------|------|-----|---------|-------|
| 企业名称 | enterpriseName | 文本 | 是 | 2-100字符 | 江苏曼斯特机电科技有限公司 |
| 统一社会信用代码 | creditCode | 文本 | 条件必填 | 18位统一社会信用代码格式 | 91320100MA1xxxxx |
| 报表期间 | reportPeriod | 日期 | 是 | YYYY-MM或YYYY-Q格式 | 2026-03 或 2026-Q1 |
| 报表类型 | reportType | 枚举 | 是 | monthly/quarterly/yearly/adhoc | quarterly |
| 数据来源 | dataSource | 枚举 | 否 | scan/electronic/manual | electronic |

---

### 4.5 字段复核模块

**页面路径**：`/pages/review/review`

#### 功能描述

**这是保证数据质量的关键环节**。系统会自动标注低置信度字段（置信度<80%），客户经理需要对这些问题字段进行逐一核对和修正。复核界面采用左右分栏设计，左侧展示字段列表，右侧展示选中字段的详细信息。

#### 功能点清单

| 序号 | 功能点 | 详细描述 | 交互细节 |
|-----|-------|---------|---------|
| F05-01 | 低置信度高亮 | 置信度<80%的字段黄色背景+警告图标显示 | 背景色 #fff8e1 |
| F05-02 | 字段映射修改 | 支持修改系统自动映射的标准字段名 | 下拉选择器 + 搜索 |
| F05-03 | 数值修正 | 支持手动修改OCR识别的错误数值 | 数字键盘输入 |
| F05-04 | 批量操作 | 支持"全部接受"/"全部标记待复核"等批量操作 | 操作栏按钮组 |
| F05-05 | 置信度详情展开 | 点击字段可展开显示原始识别文本和坐标位置 | 手风琴折叠面板 |
| F05-06 | 复核进度保存 | 支持中途保存复核进度，下次继续 | 自动保存草稿 |

#### 核心UI组件：field-table

```wxml
<!-- 字段复核表格组件 -->
<view class="field-table">
  <!-- 表头 -->
  <view class="field-header">
    <text class="col-source">源科目</text>
    <text class="col-mapped">系统字段</text>
    <text class="col-value">识别值</text>
    <text class="col-confidence">置信度</text>
    <text class="col-status">状态</text>
  </view>
  
  <!-- 字段列表 -->
  <view 
    class="field-row {{item.confidenceScore < 80 ? 'low-confidence' : ''}}"
    wx:for="{{fields}}" 
    wx:key="fieldCode"
    bindtap="onFieldTap"
    data-field="{{item}}"
  >
    <text class="col-source">{{item.sourceField}}</text>
    <text class="col-mapped">{{item.mappedField}}</text>
    <text class="col-value">{{item.recognizedValue}}</text>
    <view class="col-confidence">
      <text class="{{item.confidenceScore < 80 ? 'warning' : ''}}">
        {{item.confidenceScore}}%
      </text>
      <text wx:if="{{item.confidenceScore < 80}}" class="warning-icon">⚠️</text>
    </view>
    <text class="col-status">
      <status-tag type="{{item.reviewStatus}}" text="{{item.statusText}}"/>
    </text>
  </view>
</view>
```

#### 复核状态流转

```
┌──────────┐    自动通过     ┌──────────┐
│  待复核   │ ─────────────→ │  已确认   │
│ (Pending) │   (≥90%)       │(Confirmed)│
└──────────┘                └──────────┘
     │                            ↑
     │ 人工修正                    │
     ↓                            │
┌──────────┐    保存修正      ┌──────────┐
│  修正中   │ ─────────────→ │  已修正   │
│(Correcting)│                │(Corrected)│
└──────────┘                └──────────┘
```

---

### 4.6 三大报表展示

**页面路径**：`/pages/statements/statements`

#### 功能描述

以标准财务报表格式展示OCR识别并经复核后的三大报表数据：资产负债表、利润表、现金流量表。支持Tab切换和多级明细展开。

#### 功能点清单

| 序号 | 功能点 | 报表覆盖 | 接口依赖 |
|-----|-------|---------|---------|
| F06-01 | 资产负债表展示 | 65个标准字段（资产类35+负债权益类30） | `GET /api/financial-statements/balance-sheet/{reportId}` |
| F06-02 | 利润表展示 | 37个标准字段（收入成本费用利润） | `GET /api/financial-statements/income-statement/{reportId}` |
| F06-03 | 现金流量表展示 | 38个标准字段（经营投资筹资活动） | `GET /api/financial-statements/cash-flow/{reportId}` |

#### 报表字段覆盖详情

**资产负债表（65项）**：

| 分类 | 字段数 | 示例字段 |
|-----|-------|---------|
| 流动资产 | 15 | 货币资金、应收账款、存货、预付款项... |
| 非流动资产 | 20 | 固定资产原值、累计折旧、无形资产、长期股权投资... |
| 流动负债 | 12 | 短期借款、应付账款、应付职工薪酬、应交税费... |
| 非流动负债 | 8 | 长期借款、应付债券、长期应付款... |
| 所有者权益 | 10 | 实收资本、资本公积、未分配利润、归属于母公司权益... |

**利润表（37项）**：

| 分类 | 字段数 | 示例字段 |
|-----|-------|---------|
| 营业收入 | 5 | 营业总收入、主营业务收入、其他业务收入... |
| 营业成本 | 5 | 营业总成本、主营业务成本、税金及附加... |
| 期间费用 | 8 | 销售费用、管理费用、研发费用、财务费用... |
| 利润项目 | 19 | 营业利润、利润总额、净利润、综合收益总额... |

**现金流量表（38项）**：

| 分类 | 字段数 | 示例字段 |
|-----|-------|---------|
| 经营活动 | 16 | 销售商品收到的现金、购买商品支付的现金、支付工资... |
| 投资活动 | 10 | 收回投资收到的现金、购建固定资产支付的现金... |
| 筹资活动 | 8 | 吸收投资收到的现金、偿还债务支付的现金... |
| 汇率影响及净额 | 4 | 汇率变动影响、现金及等价物净增加额... |

---

### 4.7 数据校验模块

**页面路径**：`/pages/validation/validation`

#### 功能描述

在报表数据入库前，系统自动执行勾稽关系校验，确保三大报表之间的逻辑一致性。校验不通过的项目会标红显示差异金额，引导用户修正。

#### 功能点清单

| 序号 | 功能点 | 校验规则 | 严重级别 |
|-----|-------|---------|---------|
| F07-01 | 资产负债平衡校验 | 资产总计 = 负债合计 + 所有者权益合计 | 🔴 致命 |
| F07-02 | 利润表勾稽校验 | 营业收入 - 营业成本 = 营业毛利 | 🟡 重要 |
| F07-03 | 现金流量勾稽校验 | 现金净增加额 = 期末现金 - 期初现金 | 🟡 重要 |
| F07-04 | 跨表一致性校验 | 净利润与未分配利润变动一致 | 🟠 一般 |

#### 校验结果展示

```json
{
  "validationId": "val_001",
  "reportId": "rpt_001",
  "validatedAt": "2026-08-05T14:30:00",
  "totalChecks": 15,
  "passedCount": 12,
  "failedCount": 3,
  "results": [
    {
      "ruleName": "资产负债平衡校验",
      "ruleFormula": "资产总计 = 负债合计 + 所有者权益合计",
      "expectedValue": 7923544.71,
      "actualValue": 7923544.71,
      "difference": 0.00,
      "status": "PASSED"
    },
    {
      "ruleName": "营业毛利勾稽",
      "ruleFormula": "营业收入 - 营业成本 = 营业毛利",
      "expectedValue": 245000.00,
      "actualValue": 243500.00,
      "difference": -1500.00,
      "status": "FAILED",
      "severity": "HIGH"
    }
  ]
}
```

---

### 4.8 健康度评估模块（核心亮点）

**页面路径**：`/pages/health/health`

#### 功能描述

**这是鑫速录最具价值的智能化功能之一**。系统基于五维评分模型对企业财务状况进行全面量化评估，生成0-100分的健康度总分，并判定风险等级。小程序端采用圆环图直观展示得分和等级。

#### 五维评分模型详解

| 维度 | 权重 | 核心指标（示例） | 评估目的 |
|-----|------|----------------|---------|
| **偿债能力** | 30% | 流动比率、速动比率、资产负债率、利息保障倍数 | 能否按时还债 |
| **盈利能力** | 25% | 销售毛利率、销售净利率、ROE、ROA | 赚钱能力强弱 |
| **现金流状况** | 20% | 经营现金流/收入、现金流量比率、现金利息保障倍数 | 有没有真金白银 |
| **运营效率** | 15% | 存货周转率、应收账款周转率、总资产周转率 | 资产运转快慢 |
| **成长潜力** | 10% | 营收增长率、净利润增长率、资产增长率 | 未来发展空间 |

#### 功能点清单

| 序号 | 功能点 | 描述 | 接口依赖 |
|-----|-------|------|---------|
| F08-01 | 五维得分计算 | 基于报表数据计算五个维度得分 | `GET /api/indicators/report/{reportId}/health-score` |
| F08-02 | 总分汇总 | 加权计算健康度总分（0-100分） | 后端BigDecimal精确计算 |
| F08-03 | 风险等级判定 | 根据总分划分风险等级 | 阈值规则匹配 |
| F08-04 | 圆环图展示 | 使用自定义canvas组件绘制圆环进度图 | health-circle组件 |
| F08-05 | 维度详情查看 | 点击圆环各区域查看该维度详细指标 | 交互事件绑定 |

#### 风险等级划分标准

| 等级 | 分数区间 | 颜色标识 | 含义 | 建议措施 |
|-----|---------|---------|------|---------|
| **优秀** | ≥80分 | 🟢 绿色 | 财务状况良好 | 保持现有策略 |
| **良好** | 70-79分 | 🔵 蓝色 | 整体健康 | 关注个别短板 |
| **一般** | 60-69分 | 🟡 黄色 | 存在隐患 | 制定改善计划 |
| **需关注** | 50-59分 | 🟠 橙色 | 风险显现 | 重点监控预警 |
| **高风险** | <50分 | 🔴 红色 | 危机征兆 | 立即介入处置 |

#### 自定义组件：health-circle

```javascript
// components/health-circle/health-circle.js
Component({
  properties: {
    score: {           // 总分 0-100
      type: Number,
      value: 0
    },
    riskLevel: {       // 风险等级
      type: String,
      value: 'NORMAL'
    },
    dimensions: {      // 五维得分数组
      type: Array,
      value: []
    },
    size: {            // 尺寸
      type: Number,
      value: 200
    }
  },
  
  methods: {
    drawCircle() {
      const ctx = wx.createCanvasContext('healthCanvas', this);
      const centerX = this.data.size / 2;
      const centerY = this.data.size / 2;
      const radius = this.data.size / 2 - 20;
      
      // 绘制背景圆环
      ctx.setLineWidth(15);
      ctx.setStrokeStyle('#eef3f7');
      ctx.beginPath();
      ctx.arc(centerX, centerY, radius, 0, 2 * Math.PI);
      ctx.stroke();
      
      // 绘制进度圆环（根据分数动态设置颜色）
      const progress = this.data.score / 100;
      const color = this.getRiskColor(this.data.riskLevel);
      ctx.setStrokeStyle(color);
      ctx.setLineDash([]);
      ctx.beginPath();
      ctx.arc(centerX, centerY, radius, -Math.PI / 2, -Math.PI / 2 + 2 * Math.PI * progress);
      ctx.stroke();
      
      // 绘制中心文字
      ctx.setFillStyle(color);
      ctx.setFontSize(36);
      ctx.setTextAlign('center');
      ctx.fillText(this.data.score, centerX, centerY);
      ctx.setFontSize(14);
      ctx.fillText('分', centerX, centerY + 25);
      
      ctx.draw();
    },
    
    getRiskColor(level) {
      const colors = {
        'EXCELLENT': '#20a96b',   // 优秀-绿
        'GOOD': '#3d7cf0',        // 良好-蓝
        'NORMAL': '#f3a83b',      // 一般-黄
        'ATTENTION': '#ff9800',   // 关注-橙
        'DANGEROUS': '#e35d6a'    // 危险-红
      };
      return colors[level] || colors['NORMAL'];
    }
  }
});
```

---

### 4.9 分析报告模块

**页面路径**：`/pages/report/report`、`/pages/analysis-select/analysis-select`

#### 功能描述

系统自动生成专业格式的财务分析报告，包含总体评价、关键发现、风险分析、改进建议等内容。小程序端支持在线浏览和PDF导出。

#### 功能点清单

| 序号 | 功能点 | 描述 | 接口依赖 |
|-----|-------|------|---------|
| F09-01 | 报告列表展示 | 展示已生成的分析报告列表 | `GET /api/analysis-reports` |
| F09-02 | 报告详情查看 | 查看完整的分析报告内容 | `GET /api/analysis-reports/report/{reportId}` |
| F09-03 | 报告章节导航 | 快速定位到报告的各个章节 | 锚点滚动 |
| F09-04 | PDF导出 | 将报告导出为排版精美的PDF文件 | `GET /api/analysis-reports/export/{reportId}` |
| F09-05 | 分享转发 | 将报告分享给微信好友或群聊 | `wx.shareAppMessage` |
| F09-06 | 重新生成 | 强制重新计算指标并生成最新报告 | `POST /api/analysis-reports/generate/{reportId}` |
| F09-07 | 提交审批 | 将报告提交至后台进行审核 | `POST /api/analysis-reports/{reportId}/submit-approval` |

#### 分析报告内容结构

```markdown
# 《{企业名称}财务分析报告》

**报告期间**：{报表期}  
**生成时间**：{当前时间}  
**分析人员**：{系统/分析师}

---

## 一、执行摘要

> 一句话总结企业整体财务状况和核心建议（3-5行）

> **示例**：江苏曼斯特机电科技有限公司2026年一季度财务状况整体稳健，
> 偿债能力较强（流动比率1.85），盈利能力有待提升（销售净利率-1.29%）。
> 建议关注成本控制和营运资金管理。

---

## 二、总体评价

- **综合健康度得分**：XX分（等级：优秀/良好/一般/需关注/高风险）
- **行业对比**：高于/低于行业平均水平XX个百分点
- **主要优势**：列举2-3个突出优势
- **主要不足**：列举2-3个需要关注的方面

---

## 三、关键发现

### 3.1 积极因素 ✅
1. **偿债能力充足**：流动比率1.85，远高于安全线1.5...
2. **现金流稳定**：经营现金流保持正向...

### 3.2 风险警示 ⚠️
1. **亏损经营**：本期净利润-9,950.59元...
2. **资产负债率高企**：资产负债率达100.9%...

---

## 四、风险分析

按严重程度排列的风险清单：

| 风险项 | 影响程度 | 可能性 | 建议措施 |
|-------|---------|-------|---------|
| 偿债压力增大 | 高 | 中 | 优化负债结构... |
| 盈利能力下滑 | 中 | 高 | 加强成本管控... |

---

## 五、改进建议

针对发现的问题，提出具体的、可操作的改进建议...

---

## 六、数据质量说明

- 数据来源：OCR自动识别 + 人工复核
- 识别准确率：XX%（基于置信度加权）
- 数据完整性：140/140字段（100%）
- 最后更新时间：{时间戳}
```

---

### 4.10 趋势分析模块

**页面路径**：`/pages/trend/trend`

#### 功能描述

支持查看企业在多个报告期的财务指标变化趋势，帮助发现规律性问题和发展方向。小程序端以简洁的迷你图表形式呈现。

#### 功能点清单

| 序号 | 功能点 | 描述 | 接口依赖 |
|-----|-------|------|---------|
| F10-01 | 时间粒度切换 | 年度/季度/月度三种视图切换 | 参数传递 |
| F10-02 | 单指标趋势图 | 查看单个指标的折线图变化 | `GET /api/trends/enterprise/{enterpriseId}?indicatorCode=xxx` |
| F10-03 | 多指标对比 | 同时查看2-3个相关指标的走势 | `GET /api/trends/enterprise/{enterpriseId}/indicators` |
| F10-04 | 趋势摘要 | AI生成的趋势概览和关键发现 | `GET /api/trends/enterprise/{enterpriseId}/summary` |
| F10-05 | 同比环比计算 | 自动计算同比和环比变化率 | 后端计算逻辑 |
| F10-06 | 预警标记 | 在趋势图中标注突破阈值的时间点 | 图表特殊标记 |

#### 自定义组件：spark-line（迷你趋势图）

```javascript
// components/spark-line/spark-line.js
Component({
  properties: {
    data: {           // 数据数组 [{value: 100, label: 'Q1'}, ...]
      type: Array,
      value: []
    },
    width: {          // 宽度
      type: Number,
      value: 120
    },
    height: {         // 高度
      type: Number,
      value: 40
    },
    color: {          // 线条颜色
      type: String,
      value: '#0e8f78'
    },
    showDot: {        // 是否显示数据点
      type: Boolean,
      value: false
    }
  },
  
  methods: {
    drawSparkline() {
      const ctx = wx.createCanvasContext('sparkline', this);
      const padding = 5;
      const chartWidth = this.data.width - padding * 2;
      const chartHeight = this.data.height - padding * 2;
      
      // 计算数据范围
      const values = this.data.data.map(d => d.value);
      const minVal = Math.min(...values);
      const maxVal = Math.max(...values);
      const range = maxVal - minVal || 1;
      
      // 绘制折线
      ctx.setStrokeStyle(this.data.color);
      ctx.setLineWidth(1.5);
      ctx.beginPath();
      
      this.data.data.forEach((point, index) => {
        const x = padding + (index / (this.data.data.length - 1)) * chartWidth;
        const y = padding + chartHeight - ((point.value - minVal) / range) * chartHeight;
        
        if (index === 0) {
          ctx.moveTo(x, y);
        } else {
          ctx.lineTo(x, y);
        }
        
        // 绘制数据点
        if (this.data.showDot) {
          ctx.setFillStyle(this.data.color);
          ctx.arc(x, y, 2, 0, 2 * Math.PI);
          ctx.fill();
        }
      });
      
      ctx.stroke();
      ctx.draw();
    }
  }
});
```

---

### 4.11 报表归档模块

**页面路径**：`/pages/archive-list/archive-list`、`/pages/archive/archive`

#### 功能描述

对所有已完成处理的报表进行归档管理，支持按企业、期间、状态等多维度检索和筛选。

#### 功能点清单

| 序号 | 功能点 | 描述 | 接口依赖 |
|-----|-------|------|---------|
| F11-01 | 归档列表展示 | 分页展示已归档的报表列表 | `GET /api/reports` |
| F11-02 | 多条件筛选 | 按企业、期间、状态、风险等级筛选 | 查询参数组合 |
| F11-03 | 关键词搜索 | 搜索企业名称或信用代码 | 模糊匹配 |
| F11-04 | 归档详情查看 | 查看报表完整信息和关联数据 | `GET /api/reports/{id}` |
| F11-05 | 删除归档 | 逻辑删除已归档报表（需权限） | `DELETE /api/reports/{id}` |

---

### 4.12 个人中心模块

**页面路径**：`/pages/profile/profile`

#### 功能点清单

| 序号 | 功能点 | 描述 |
|-----|-------|------|
| F12-01 | 用户信息展示 | 显示头像、姓名、角色、所属机构 |
| F12-02 | 修改密码 | 支持修改登录密码 |
| F12-03 | 统计概览 | 展示个人处理的报表数量、本周新增等统计 |
| F12-04 | 关于我们 | 产品版本、技术支持联系方式 |
| F12-05 | 退出登录 | 安全退出并清除缓存 |

---

## 5. Vue管理后台功能需求

### 5.1 登录认证模块

**路由路径**：`/login`  
**组件文件**：`views/Login.vue`

#### 功能描述

PC端管理后台的统一入口，提供用户身份验证和会话管理功能。

#### 功能点清单

| 序号 | 功能点 | UI元素 | 接口依赖 |
|-----|-------|-------|---------|
| L01-01 | 登录表单 | 居中卡片式表单 | `POST /api/auth/login` |
| L01-02 | 表单验证 | 非空校验、格式校验 | Element UI Form校验 |
| L01-03 | 记住密码 | Checkbox选项 | localStorage持久化 |
| L01-04 | 登录状态维持 | Vuex Store + sessionStorage | Token自动附加 |
| L01-05 | 路由守卫 | 未登录重定向到登录页 | Router beforeEach钩子 |
| L01-06 | 权限控制 | 根据角色隐藏菜单项 | 动态路由生成 |

#### 默认账号

| 角色 | 用户名 | 密码 | 权限范围 |
|-----|-------|------|---------|
| 系统管理员 | admin | admin123 | 全部功能 |
| 客户经理 | manager | manager123 | 采集、复核、查看 |

---

### 5.2 工作台仪表盘（A01）

**路由路径**：`/`  
**组件文件**：`views/Dashboard.vue`

#### 功能描述

系统首页，展示核心业务KPI、数据处理统计、待办任务列表和关键图表，帮助管理者快速掌握全局运营状况。

#### UI布局结构

```
┌──────────────────────────────────────────────────────────────────┐
│  A01 工作台                                                      │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │本月新增  │ │待人工复核│ │OCR准确率 │ │高风险企业│           │
│  │  86份 ↑  │ │ 12项 ↓  │ │ 97.4% ↑  │ │ 9家  ↑   │           │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
│                                                                  │
│  ┌─────────────────────────────┐ ┌─────────────────────────────┐ │
│  │                             │ │                             │ │
│  │  报表处理量与通过率          │ │  风险等级分布               │ │
│  │  （双折线图）                │ │  （环形图）                  │ │
│  │                             │ │                             │ │
│  └─────────────────────────────┘ └─────────────────────────────┘ │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  待处理任务                                                  │ │
│  │  ┌──────┬──────────┬──────┬──────┬──────────┬────┬─────┐   │ │
│  │  │编号  │企业名称  │类型  │优先级│创建时间  │状态│操作 │   │ │
│  │  ├──────┼──────────┼──────┼──────┼──────────┼────┼─────┤   │ │
│  │  │T001  │某某公司  │OCR   │高    │08-05 14:30│待办│查看 │   │ │
│  │  └──────┴──────────┴──────┴──────┴──────────┴────┴─────┘   │ │
│  │                              [ 查看全部 ]                    │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

#### KPI指标卡定义

| 卡片名称 | 指标内容 | 趋势标识 | 颜色方案 | 数据来源 |
|---------|---------|---------|---------|---------|
| 本月新增报表 | 86份 | ↑18.6% | 绿色上升 | `GET /api/dashboard/kpis` |
| 待人工复核 | 12项 ↓4项高优 | 红色警告 | 红色警示 | `GET /api/dashboard/tasks` |
| OCR平均准确率 | 97.4% | ↑1.2pct | 绿色提升 | 统计计算 |
| 高风险企业 | 9家 ↓较上月+2 | 橙色注意 | 橙色提醒 | `GET /api/dashboard/statistics` |

#### 图表组件

**左侧 - 报表处理量与通过率（双折线图）**

```javascript
// charts/LineChart.vue 配置
{
  xAxis: {
    type: 'category',
    data: ['1月', '2月', '3月', ..., '12月']  // 近12个月
  },
  yAxis: [
    { name: '处理量（份）', position: 'left' },
    { name: '通过率（%）', position: 'right' }
  ],
  series: [
    { name: '处理量', type: 'line', data: [...] },      // 蓝色曲线
    { name: '通过率', type: 'line', yAxisIndex: 1, data: [...] }  // 绿色曲线
  ]
}
```

**右侧 - 风险等级分布（环形图）**

```javascript
// charts/PieChart.vue 配置
{
  series: [{
    type: 'pie',
    radius: ['45%', '70%'],  // 环形
    data: [
      { value: 80, name: '低风险', itemStyle: {color: '#20a96b'} },
      { value: 25, name: '中风险', itemStyle: {color: '#f3a83b'} },
      { value: 15, name: '较高风险', itemStyle: {color: '#ff9800'} },
      { value: 6, name: '高风险', itemStyle: {color: '#e35d6a'} }
    ],
    label: {
      formatter: '{b}\n{c}家 ({d}%)'
    }
  }]
}
```

#### API接口依赖

| 接口 | 方法 | 用途 | 调用时机 |
|-----|------|------|---------|
| `/api/dashboard/kpis` | GET | 获取KPI指标数据 | 页面加载 |
| `/api/dashboard/chart-data?type=volume` | GET | 获取处理量图表数据 | 页面加载 |
| `/api/dashboard/chart-data?type=risk` | GET | 获取风险分布数据 | 页面加载 |
| `/api/dashboard/tasks?limit=5` | GET | 获取待办任务列表 | 页面加载 |
| `/api/dashboard/statistics` | GET | 获取统计数据 | 页面加载 |

---

### 5.3 企业与报表管理（A02）

**路由路径**：`/enterprise`  
**组件文件**：`views/EnterpriseList.vue`

#### 功能描述

企业管理主界面，支持企业的增删改查、报表关联管理和多维度数据筛选。

#### 功能点清单

| 序号 | 功能点 | UI元素 | 详细描述 |
|-----|-------|-------|---------|
| E01-01 | 企业列表表格 | DataTable组件 | 展示企业名称、最新报表期、三表状态、健康度、风险标签、客户经理、更新时间 |
| E01-02 | 多条件筛选 | 筛选工具栏 | 支持关键词搜索、期间筛选、风险等级多选、数据状态下拉 |
| E01-03 | 新增企业 | 对话框表单 | 弹窗式表单，录入企业基本信息 |
| E01-04 | 编辑企业 | 行内编辑/对话框 | 修改企业信息 |
| E01-05 | 删除企业 | 确认对话框 | 逻辑删除，二次确认 |
| E01-06 | 导出Excel | 工具栏按钮 | 导出当前筛选结果为Excel文件 |
| E01-07 | 查看详情 | 行点击跳转 | 跳转到报表详情页 `/enterprise/:id` |
| E01-08 | 分页控件 | 底部分页器 | 支持10/20/50条每页切换 |

#### 表格列定义

| 列名 | 字段编码 | 宽度 | 排序 | 渲染方式 |
|-----|---------|------|-----|---------|
| 企业名称 | enterpriseName | 180px | - | 可点击链接 |
| 最新报表期 | latestReportPeriod | 120px | 支持 | 格式化显示 |
| 三表状态 | statementStatus | 150px | - | 三色圆点图标组 |
| 健康度 | healthScore | 120px | 支持 | 进度条 + 数值 |
| 关键风险 | keyRisks | 200px | - | Tag标签云 |
| 客户经理 | managerName | 100px | 支持 | 文本 |
| 更新时间 | updatedAt | 150px | 支持 | 时间格式化 |
| 操作 | actions | 150px | - | 查看/编辑/删除按钮组 |

#### 底部统计区域

```
共126家企业 / 368期报表

┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│已完成填报  │ │ 待复核   │ │ 高风险   │ │本月新增  │
│  98家    │ │  18家    │ │   9家    │ │  15家    │
└──────────┘ └──────────┘ └──────────┘ └──────────┘
```

---

### 5.4 报表详情与OCR复核（A03）

**路由路径**：`/enterprise/:id`  
**组件文件**：`views/ReportDetail.vue`

#### 功能描述

单张报表的详细信息和OCR识别结果复核工作台。这是数据质量控制的核心界面，采用Tab切换 + 左右分栏的专业布局。

#### UI布局结构

```
┌──────────────────────────────────────────────────────────────────┐
│  企业与报表 > [企业名] > [报表期]                    [返回列表]   │
├──────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ 企业全称：江苏曼斯特机电科技有限公司                          │ │
│  │ 报表期间：2026年一季度    任务编号：TASK-20260805-001        │ │
│  │ 数据来源：电子版PDF      提交时间：2026-08-05 14:30:00      │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  [资产负债表] [利润表] [现金流量表] [校验结果汇总]                  │
│  ─────────────────────────────────────────────────────────────  │
│                                                                  │
│  ┌──────────────────────────────┬───────────────────────────────┐│
│  │                              │                               ││
│  │      字段映射表格（60%）      │      详情面板（40%）          ││
│  │                              │                               ││
│  │  ┌────┬──────┬────┬────┬──┐ │  选中字段：货币资金            ││
│  │  │源科│源行次│系统│置信│状│ │  ─────────────────────        ││
│  │  │目  │      │字段│度  │态│ │  目标字段：[货币资金 ▼]       ││
│  │  ├────┼──────┼────┼────┼──┤ │                               ││
│  │  │货币│  1   │货币│98% │✓ │ │  识别值：1,034,959.57         ││
│  │  │资金│      │资金│    │   │ │  原始文本：货币资金            ││
│  │  ├────┼──────┼────┼────┼──┤ │  坐标位置：(120.5, 89.3)     ││
│  │  │应收│  5   │应收│75% │⚠️│ │                               ││
│  │  │账款│      │账款│    │   │ │  ┌─────────────────────┐     ││
│  │  │    │      │    │    │   │ │  │ 勾稽校验结果        │     ││
│  │  └────┴──────┴────┴────┴──┘ │  │ 公式：流动资产=...   │     ││
│  │                              │  │ 预期值：xxx          │     ││
│  │  低置信度字段黄色背景高亮     │  │ 实际值：xxx          │     ││
│  │                              │  │ 差异：xxx            │     ││
│  │                              │  │ 状态：✓ 通过         │     ││
│  │                              │  └─────────────────────┘     ││
│  └──────────────────────────────┴───────────────────────────────┘│
│                                                                  │
│  [保存复核结果] [提交审核] [重新OCR]                                │
└──────────────────────────────────────────────────────────────────┘
```

#### Tab页签功能

| Tab名称 | 内容 | 核心操作 |
|---------|------|---------|
| **资产负债表** | 65个字段的映射表格 | 字段修正、映射调整 |
| **利润表** | 37个字段的映射表格 | 数值校对、公式验证 |
| **现金流量表** | 38个字段的映射表格 | 流向核查、金额确认 |
| **校验结果汇总** | 勾稽校验结果列表 | 查看差异、修正源头 |

#### 右侧详情面板交互

- **目标字段选择器**：下拉搜索，支持模糊匹配
- **数值快速修正**：数字输入框，支持千分位格式
- **勾稽校验展示**：
  - 校验公式显示
  - 计算结果对比
  - 通过/不通过状态标识（绿色✓ / 红色✗）
  - 差异金额标红显示

---

### 5.5 财务分析报告（A04）

**路由路径**：`/analysis`  
**组件文件**：`views/AnalysisReport.vue`

#### 功能描述

AI驱动的综合财务分析报告生成与展示界面。采用渐变KPI卡 + 雷达图 + 风险列表 + Markdown文本区的复合布局。

#### UI布局结构

```
┌──────────────────────────────────────────────────────────────────┐
│  A04 财务分析报告                                    [选择企业 ▼] │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────┐│
│  │ 综合健康度   │ │  流动比率    │ │ 资产负债率   │ │经营现金  ││
│  │   82.5分    │ │   1.85      │ │   45.2%     │ │ /收入    ││
│  │ 🟢 优秀     │ │ 🟢 正常     │ │ 🟢 健康     │ │🟢 充裕  ││
│  │ vs 75.0分   │ │ vs 1.50     │ │ vs 60.0%    │ │vs 8.0%  ││
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────────┘│
│                                                                  │
│  ┌─────────────────────────────┐ ┌─────────────────────────────┐ │
│  │                             │ │                             │ │
│  │      五维雷达图             │ │      关键风险排序            │ │
│  │      (RadarChart)           │ │      (Card List)            │ │
│  │                             │ │                             │ │
│  │        偿债能力             │ │  🔴 偿债压力增大             │ │
│  │          ╱╲                │ │     影响程度：高             │ │
│  │        ╱  ╲  成长           │ │     建议措施：优化负债...    │ │
│  │       ╱────╲                │ │                             │ │
│  │      ╱  盈利╲               │ │  🟠 盈利能力下滑             │ │
│  │     ╱  运营  ╲              │ │     影响程度：中             │ │
│  │     ╲  效率  ╱              │ │     建议措施：加强成本...    │ │
│  │      ╲────╱                │ │                             │ │
│  │       ╲  现金╱              │ │  🟡 存货周转放缓             │ │
│  │        ╲  ╱                │ │     影响程度：一般           │ │
│  │         ╲╱                 │ │     建议措施：优化库存...    │ │
│  │                             │ │                             │ │
│  └─────────────────────────────┘ └─────────────────────────────┘ │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  AI分析摘要（Markdown渲染）                                   │ │
│  │                                                              │ │
│  │  ## 总体评价                                                 │ │
│  │  该企业整体财务状况**良好**，偿债能力较强...                   │ │
│  │                                                              │ │
│  │  ### 优势分析                                                │ │
│ │  1. 流动比率1.85，远高于行业均值1.50...                       │ │
│  │                                                              │ │
│  │  ### 风险提示                                                │ │
│  │  1. ⚠️ 本期出现亏损，需关注持续性...                          │ │
│  │                                                              │ │
│  │  [复制全文]  [导出PDF]  [打印报告]                             │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

#### 核心组件

**KPI指标卡（KpiCard.vue）**

```vue
<template>
  <div class="kpi-card" :class="trendClass">
    <div class="kpi-icon">
      <i :class="iconClass"></i>
    </div>
    <div class="kpi-content">
      <div class="kpi-label">{{ label }}</div>
      <div class="kpi-value">{{ formattedValue }}</div>
      <div class="kpi-compare">
        <span class="benchmark">行业均值: {{ benchmark }}</span>
        <span class="trend" :class="trendDirection">
          {{ trendText }}
        </span>
      </div>
      <div class="kpi-status">
        <el-tag :type="statusType" size="small">{{ statusText }}</el-tag>
      </div>
    </div>
  </div>
</template>
```

**五维雷达图（RadarChart.vue）**

```javascript
// ECharts雷达图配置
{
  radar: {
    indicator: [
      { name: '偿债能力', max: 100 },
      { name: '盈利能力', max: 100 },
      { name: '运营效率', max: 100 },
      { name: '现金流状况', max: 100 },
      { name: '成长潜力', max: 100 }
    ],
    shape: 'polygon',
    splitNumber: 5,
    axisName: { color: '#6c7d89' }
  },
  series: [{
    type: 'radar',
    data: [{
      value: [85, 72, 68, 78, 65],  // 五维得分
      name: '当前企业',
      areaStyle: { color: 'rgba(14, 143, 120, 0.3)' },
      lineStyle: { color: '#0e8f78' },
      itemStyle: { color: '#0e8f78' }
    }, {
      value: [75, 70, 65, 70, 60],  // 行业平均
      name: '行业平均',
      areaStyle: { color: 'rgba(108, 125, 137, 0.1)' },
      lineStyle: { color: '#6c7d89', type: 'dashed' },
      itemStyle: { color: '#6c7d89' }
    }]
  }]
}
```

---

### 5.6 历史趋势监控（A05）

**路由路径**：`/trend`  
**组件文件**：`views/TrendMonitor.vue`

#### 功能描述

多时间维度的财务指标历史变化追踪界面，支持丰富的图表类型和交互式数据探索。

#### 功能点清单

| 序号 | 功能点 | UI元素 | 描述 |
|-----|-------|-------|------|
| T01-01 | 时间粒度切换 | 单选按钮组 | 年度/季度/月度三种视图 |
| T01-02 | 时间范围选择 | 下拉选择器 | 近3年/近5年/全部 |
| T01-03 | 企业多选 | 下拉多选框 | 支持同时查看多家企业对比 |
| T01-04 | 盈利能力趋势图 | 折线图 | 主指标：销售毛利率，辅助线：行业平均 |
| T01-05 | 杠杆与流动性图 | 双Y轴折线图 | 资产负债率（左轴）+ 流动比率（右轴） |
| T01-06 | 指标卡片网格 | 4列网格 | 包含迷你趋势图（Sparkline） |
| T01-07 | 预警记录表格 | 数据表格 | 预警时间、指标、当前值、阈值、级别、状态 |
| T01-08 | 应用筛选按钮 | 主按钮 | 触发数据刷新 |

#### 指标卡片定义

| 卡片 | 指标 | 当前值 | 趋势 | 同比变化 |
|-----|------|-------|------|---------|
| 卡片1 | 销售增长率 | 15.8% | ↑ | +3.2pct |
| 卡片2 | 销售毛利率 | 28.5% | ↑ | +1.8pct |
| 卡片3 | 资产负债率 | 45.2% | ↓ | -5.1pct |
| 卡片4 | 流动比率 | 1.85 | → | 持平 |

---

### 5.7 规则配置中心（A06）

**路由路径**：`/rule-config`  
**组件文件**：`views/RuleConfig.vue`

#### 功能描述

**这是系统灵活性的核心体现**。管理员可以根据具体业务需要，设定各项财务指标的风险阈值、评估权重和评级标准，真正做到灵活可控。

#### 功能点清单

| 序号 | 功能点 | 描述 | 交互方式 |
|-----|-------|------|---------|
| R01-01 | 规则列表展示 | 展示所有已配置的指标规则 | 可编辑表格 |
| R01-02 | 阈值区间设置 | 为每个指标设置正常/关注/高风险三个阈值 | 数字输入框 |
| R01-03 | 权重占比调整 | 调整五维健康度各维度权重（总和=100%） | 滑块拖动 |
| R01-04 | 规则启用/停用 | Switch开关控制规则是否生效 | 开关组件 |
| R01-05 | 新增规则 | 弹窗表单添加新的指标规则 | 对话框 |
| R01-06 | 编辑规则 | 修改已有规则的参数 | 行内编辑 |
| R01-07 | 删除规则 | 删除不再需要的规则 | 确认对话框 |
| R01-08 | 规则测试 | 输入模拟值查看评级结果 | 测试按钮+弹窗 |
| R01-09 | 导入/导出 | Excel模板批量导入导出 | 文件操作按钮 |
| R01-10 | 健康度权重配置 | 可视化权重分配界面 | 进度条+拖拽 |
| R01-11 | 规则治理说明 | 评分方法论、阈值依据等文档说明 | 可折叠面板 |

#### 规则配置表格列定义

| 列名 | 说明 | 编辑方式 | 示例值 |
|-----|------|---------|-------|
| 指标名称 | 如"流动比率" | 只读 | 流动比率 |
| 计算公式 | 如"流动资产/流动负债" | 只读展示 | 流动资产 / 流动负债 |
| 正常阈值 | 绿色区间 | 数字输入 | >= 2.0 |
| 关注阈值 | 黄色区间 | 数字输入 | 1.5 - 2.0 |
| 高风险阈值 | 红色区间 | 数字输入 | < 1.5 |
| 权重占比 | 在维度内的权重 | 滑块(0-100%) | 25% |
| 状态 | 启用/停用 | Switch开关 | 启用 |
| 操作 | 编辑/删除/测试 | 按钮组 | - |

#### 底部配置区域

**左侧 - 健康度权重配置**

```
偿债能力  ████████████████████░░░░  30%  ─●──
盈利能力  ████████████████░░░░░░░  25%  ───●─
现金流   ██████████████░░░░░░░░░  20%  ────●
运营效率  ████████████░░░░░░░░░░░  15%  ─────●
成长潜力  ██████████░░░░░░░░░░░░░  10%  ──────●
                                  ────────
                                  总和: 100% ✓

[ 保存配置 ]
```

**右侧 - 规则治理原则说明**

```
📖 规则治理原则

▸ 评分方法论
  采用加权平均法计算各维度得分，再汇总为总分...

▸ 阈值设定依据
  参考国家统计局行业标准、国资委考核指标...

▸ 行业对标标准
  支持按制造业/服务业/科技业等设置不同基准...

▸ 更新维护规范
  建议每季度回顾一次规则配置...
```

---

### 5.8 审计日志管理

**路由路径**：（嵌套在其他页面中）  
**组件文件**：独立的管理页面或侧边抽屉

#### 功能描述

记录系统中所有关键操作的审计日志，支持操作追溯和安全审计。

#### 日志记录范围

| 操作类型 | 记录内容 | 示例 |
|---------|---------|------|
| 登录登出 | 用户、时间、IP、设备 | 用户admin于2026-08-05 14:30登录 |
| 数据修改 | 修改前后值、操作人、时间 | 修改报表rpt_001的流动比率：1.5→1.85 |
| 报表操作 | 上传、OCR、复核、提交 | 用户manager提交报表rpt_001审核 |
| 规则变更 | 修改内容、新旧值、操作人 | 修改流动比率的正常阈值：2.0→1.8 |
| 导出操作 | 导出内容、格式、操作人 | 导出企业xxx的分析报告PDF |

---

## 6. 后端API服务需求

### 6.1 认证授权API

**基础路径**：`/api/auth`

| 方法 | 路径 | 功能 | 请求参数 | 响应格式 |
|-----|------|------|---------|---------|
| POST | `/login` | 用户登录 | `{username, password}` | `{token, userInfo}` |
| POST | `/logout` | 用户登出 | - | `{success: true}` |
| GET | `/userinfo` | 获取用户信息 | Header: Authorization | `{roles, permissions, name}` |
| POST | `/refresh-token` | 刷新Token | `{refreshToken}` | `{newToken}` |

---

### 6.2 文件上传API

**基础路径**：`/api/files`

| 方法 | 路径 | 功能 | 请求参数 | 响应格式 |
|-----|------|------|---------|---------|
| POST | `/upload` | 上传文件 | FormData: file | `{fileId, fileName, url, size}` |
| GET | `/{fileId}` | 获取文件信息 | fileId | `{file}` |
| GET | `/{fileId}/download` | 下载文件 | fileId | 文件流 |
| DELETE | `/{fileId}` | 删除文件 | fileId | `{success: true}` |

---

### 6.3 OCR识别API（核心）

**基础路径**：`/api/ocr`

| 方法 | 路径 | 功能 | 请求参数 | 响应格式 |
|-----|------|------|---------|---------|
| POST | `/recognize` | **同步OCR识别** | `fileId` | `{OcrTask}` (含完整结果) |
| POST | `/recognize-async` | **异步OCR识别** | `fileId` | `{OcrTask}` (仅任务ID) |
| GET | `/tasks/{taskId}` | 查询任务状态 | taskId | `{taskStatus, progress}` |
| GET | `/tasks/{taskId}/results` | 获取识别结果 | taskId | `{fields[], confidence}` |
| POST | `/tasks/merge` | 合并多张OCR结果 | `[taskIds]` | `{mergedTask}` |
| GET | `/tasks` | OCR任务列表 | `page, size` | `Page<OcrTask>` |
| DELETE | `/tasks/{taskId}` | 取消任务 | taskId | `{success: true}` |

#### OCR识别流程详解

```java
/**
 * OcrServiceImpl.recognize() 核心逻辑
 */
public OcrTask recognize(UploadedFile file) {
    // 1. 创建OCR任务记录
    OcrTask task = createOcrTask(file);
    
    // 2. 根据配置选择识别引擎
    if ("mock".equals(aiProvider)) {
        // 本地PDF解析模式（适用于带文字层的标准PDF）
        fieldResults = financialReportPdfExtractor.extract(task, file);
    } else if ("http".equals(aiProvider)) {
        // 远程HTTP OCR模式（适用于扫描件/图片）
        fieldResults = callHttpOcrService(file);
    }
    
    // 3. 字段标准化映射
    fieldResults = financialReportFieldNormalizer.normalize(fieldResults);
    
    // 4. 保存识别结果
    ocrFieldResultRepository.saveAll(fieldResults);
    
    // 5. 更新任务统计
    updateTaskStatistics(task, fieldResults);
    
    return task;
}
```

#### PDF本地解析技术细节

```java
/**
 * FinancialReportPdfExtractor.extract() 核心算法
 */
public List<OcrFieldResult> extract(OcrTask task, File file) {
    // 1. 使用PDFBox加载PDF文档
    try (PDDocument document = PDDocument.load(file)) {
        
        // 2. 提取文本内容和位置坐标
        PositionedTextStripper stripper = new PositionedTextStripper();
        stripper.setSortByPosition(true);  // 按位置排序
        
        // 3. 逐页解析
        for (int page = 1; page <= pages; page++) {
            List<PositionedLine> lines = extractPageLines(document, page);
            
            // 4. 匹配财务字段模板（140个字段定义）
            for (FieldDefinition def : FinancialReportFieldTemplates.all()) {
                ExtractedValue value = extractValue(lines, def, pageWidth);
                
                // 5. 坐标级精确定位提取金额
                if (value.labelFound) {
                    fields.add(buildField(task, def, value, confidence));
                }
            }
        }
    }
    
    return fields;  // 返回140个字段的识别结果
}
```

---

### 6.4 企业管理API

**基础路径**：`/api/enterprises`

| 方法 | 路径 | 功能 | 请求参数 | 响应格式 |
|-----|------|------|---------|---------|
| GET | `` | 企业列表 | `page, size, keyword, riskLevel, status` | `Page<Enterprise>` |
| GET | `/{id}` | 企业详情 | id | `{enterprise, reports[]}` |
| POST | `` | 新增企业 | `{enterpriseData}` | `{id}` |
| PUT | `/{id}` | 更新企业 | `id, {data}` | `{success}` |
| DELETE | `/{id}` | 删除企业 | id | `{success}` |
| GET | `/export` | 导出企业列表 | filters | Blob(Excel) |

---

### 6.5 财务报表API

**基础路径**：`/api/financial-statements`

| 方法 | 路径 | 功能 | 请求参数 | 响应格式 |
|-----|------|------|---------|---------|
| GET | `/balance-sheet/{reportId}` | 获取资产负债表 | reportId | `{BalanceSheet + Items[]}` |
| GET | `/income-statement/{reportId}` | 获取利润表 | reportId | `{IncomeStatement + Items[]}` |
| GET | `/cash-flow/{reportId}` | 获取现金流量表 | reportId | `{CashFlowStatement + Items[]}` |
| POST | `/balance-sheet/{reportId}` | 保存资产负债表 | reportId, `Items[]` | `{success}` |
| POST | `/income-statement/{reportId}` | 保存利润表 | reportId, `Items[]` | `{success}` |
| POST | `/cash-flow/{reportId}` | 保存现金流量表 | reportId, `Items[]` | `{success}` |

---

### 6.6 指标计算API（核心）

**基础路径**：`/api/indicators`

| 方法 | 路径 | 功能 | 响应内容 |
|-----|------|------|---------|
| GET | `/report/{reportId}` | **获取30+财务指标** | `{currentRatio: 1.85, roe: 0.152, ...}` |
| GET | `/report/{reportId}/health-score` | **获取健康度评分** | `{HealthScoreVO}` |
| GET | `/definitions` | 获取指标定义和公式 | `{solvency: {...}, profitability: {...}, ...}` |

#### 30+财务指标完整清单

**短期偿债能力（5项）**：
- 流动比率 (currentRatio) = 流动资产 / 流动负债
- 速动比率 (quickRatio) = (流动资产 - 存货) / 流动负债
- 保守速动比率 (conservativeQuickRatio) = (货币资金 + 应收票据 + 应收账款) / 流动负债
- 现金比率 (cashRatio) = 货币资金 / 流动负债
- 营运资本 (workingCapital) = 流动资产 - 流动负债

**长期偿债能力（5项）**：
- 资产负债率 (debtToAssetRatio) = 总负债 / 总资产 × 100%
- 产权比率 (debtToEquityRatio) = 总负债 / 所有者权益
- 有形净值债务率 (tangibleDebtRatio) = 总负债 / (所有者权益 - 无形资产)
- 利息保障倍数 (interestCoverageRatio) = (利润总额 + 利息支出) / 利息支出
- 长期债务与营运资金比率 (longTermDebtToWorkingCapital) = 非流动负债 / (流动资产 - 流动负债)

**盈利能力（6项）**：
- 销售毛利率 (grossProfitMargin) = (营业收入 - 营业成本) / 营业收入 × 100%
- 营业利润率 (operatingProfitMargin) = 营业利润 / 营业收入 × 100%
- 销售净利率 (netProfitMargin) = 净利润 / 营业收入 × 100%
- 成本费用利润率 (costExpenseProfitMargin) = 利润总额 / 成本费用总额 × 100%
- 总资产收益率ROA (roa) = 净利润 / 平均总资产 × 100%
- 净资产收益率ROE (roe) = 净利润 / 平均所有者权益 × 100%

**运营效率（7项）**：
- 应收账款周转率 (accountsReceivableTurnover) = 营业收入 / 平均应收账款
- 应收账款周转天数 (accountsReceivableTurnoverDays) = 365 / 应收账款周转率
- 存货周转率 (inventoryTurnover) = 营业成本 / 平均存货
- 存货周转天数 (inventoryTurnoverDays) = 365 / 存货周转率
- 流动资产周转率 (currentAssetTurnover) = 营业收入 / 平均流动资产
- 固定资产周转率 (fixedAssetTurnover) = 营业收入 / 固定资产净值
- 总资产周转率 (totalAssetTurnover) = 营业收入 / 平均总资产

**现金流能力（5项）**：
- 现金流量比率 (cashFlowRatio) = 经营活动现金流净额 / 流动负债
- 现金负债比率 (cashDebtRatio) = 经营活动现金流净额 / 总负债
- 经营现金净流量与净利润比率 (operatingCashToNetProfit) = 经营现金流净额 / 净利润
- 经营现金净流量与营业收入比率 (operatingCashToRevenue) = 经营现金流净额 / 营业收入
- 现金流量利息保障倍数 (cashFlowInterestCoverage) = 经营现金流净额 / 利息支出

**成长能力（5项）**：
- 营业收入增长率 (revenueGrowthRate) = (本期营收 - 上期营收) / 上期营收 × 100%
- 净利润增长率 (netProfitGrowthRate) = (本期净利润 - 上期净利润) / 上期净利润 × 100%
- 总资产增长率 (totalAssetGrowthRate) = (本期总资产 - 上期总资产) / 上期总资产 × 100%
- 所有者权益增长率 (equityGrowthRate) = (本期权益 - 上期权益) / 上期权益 × 100%
- 经营现金流增长率 (operatingCashFlowGrowthRate) = (本期经营现金流 - 上期经营现金流) / 上期经营现金流 × 100%

#### 健康度评分算法

```java
/**
 * FinancialReportServiceImpl.calculateHealthScore()
 * 五维评分模型核心算法
 */
public HealthScoreVO calculateHealthScore(Long reportId) {
    // 1. 获取指标数据
    Map<String, Object> indicators = calculateIndicators(reportId);
    
    // 2. 获取权重配置（可动态调整）
    BigDecimal solvencyWeight = getDimensionWeight("solvency", "0.30");
    BigDecimal profitabilityWeight = getDimensionWeight("profitability", "0.25");
    BigDecimal cashFlowWeight = getDimensionWeight("cashFlow", "0.20");
    BigDecimal operationWeight = getDimensionWeight("operation", "0.15");
    BigDecimal growthWeight = getDimensionWeight("growth", "0.10");
    
    // 3. 计算各维度得分（基于阈值规则）
    BigDecimal solvencyScore = calculateDimensionScore("solvency", indicators);   // 0-100
    BigDecimal profitabilityScore = calculateDimensionScore("profitability", indicators);
    BigDecimal cashFlowScore = calculateDimensionScore("cashFlow", indicators);
    BigDecimal operationScore = calculateDimensionScore("operation", indicators);
    BigDecimal growthScore = calculateDimensionScore("growth", indicators);
    
    // 4. 加权汇总
    BigDecimal totalScore = solvencyScore.multiply(solvencyWeight)
        .add(profitabilityScore.multiply(profitabilityWeight))
        .add(cashFlowScore.multiply(cashFlowWeight))
        .add(operationScore.multiply(operationWeight))
        .add(growthScore.multiply(growthWeight));
    
    // 5. 判定风险等级
    String riskLevel = determineRiskLevel(totalScore);
    
    // 6. 组装返回对象
    HealthScoreVO vo = new HealthScoreVO();
    vo.setTotalScore(totalScore.setScale(2, RoundingMode.HALF_UP));
    vo.setSolvencyScore(solvencyScore);
    vo.setProfitabilityScore(profitabilityScore);
    vo.setCashFlowScore(cashFlowScore);
    vo.setOperationScore(operationScore);
    vo.setGrowthScore(growthScore);
    vo.setRiskLevel(riskLevel);
    vo.setSummary(generateHealthSummary(vo));  // AI生成摘要
    
    // 7. 持久化
    saveHealthScore(reportId, vo);
    
    return vo;
}
```

---

### 6.7 分析报告API

**基础路径**：`/api/analysis-reports`

| 方法 | 路径 | 功能 | 响应格式 |
|-----|------|------|---------|
| GET | `/report/{reportId}` | 获取分析报告 | `{AnalysisReportVO}` |
| POST | `/generate/{reportId}` | **重新生成报告** | `{AnalysisReportVO}` |
| POST | `/{reportId}/submit-approval` | 提交报告审批 | `{AnalysisReportVO}` |
| GET | `/export/{reportId}` | **导出PDF报告** | ResponseEntity(PDF) |

#### AnalysisReportVO 结构

```json
{
  "archiveId": 1,
  "enterpriseId": 1,
  "enterpriseName": "江苏曼斯特机电科技有限公司",
  "reportPeriod": "2026-Q1",
  "healthScore": 82.5,
  "riskLevel": "GOOD",
  
  "executiveSummary": "该企业整体财务状况良好...",
  "overallAssessment": "综合健康度82.5分，处于良好水平...",
  "keyFindings": ["偿债能力充足...", "盈利能力有待提升..."],
  "riskAnalysis": [{ "risk": "...", "severity": "HIGH", "suggestion": "..." }],
  "positiveFactors": ["流动比率1.85...", "经营现金流稳定..."],
  "improvementSuggestions": ["建议关注成本控制...", "优化营运资金管理..."],
  "dataQualityNotes": "数据来源：OCR识别，准确率96.8%...",
  
  "status": "generated",
  "version": 1,
  "createdTime": "2026-08-05T14:30:00"
}
```

---

### 6.8 趋势分析API

**基础路径**：`/api/trends`

| 方法 | 路径 | 功能 | 响应格式 |
|-----|------|------|---------|
| GET | `/enterprise/{enterpriseId}` | 获取单指标趋势 | `{TrendVO}` |
| GET | `/enterprise/{enterpriseId}/indicators` | **多指标对比趋势** | `List<TrendVO>` |
| GET | `/enterprise/{enterpriseId}/summary` | **趋势摘要** | `{overallTrend, keyFindings[]}` |

#### TrendVO 结构

```json
{
  "indicatorCode": "roe",
  "indicatorName": "净资产收益率",
  "enterpriseId": 1,
  "unit": "%",
  "dataList": [
    { "period": "2025-Q1", "value": 12.5, "yoyChange": 2.3 },
    { "period": "2025-Q2", "value": 13.2, "yoyChange": 3.1 },
    { "period": "2025-Q3", "value": 14.0, "yoyChange": 3.8 },
    { "period": "2025-Q4", "value": 14.8, "yoyChange": 4.2 },
    { "period": "2026-Q1", "value": 15.2, "yoyChange": 5.1 }
  ],
  "trendDirection": "IMPROVING",  // IMPROVING/DECLINING/STABLE
  "benchmark": 12.0,              // 行业基准值
  "latestVsBenchmark": "+3.2pct"  // 与基准对比
}
```

---

### 6.9 规则配置API

**基础路径**：`/api/indicators/rules` 或独立的规则控制器

| 方法 | 路径 | 功能 | 请求体 |
|-----|------|------|-------|
| GET | `` | 获取规则列表 | - |
| GET | `/{ruleId}` | 获取规则详情 | - |
| POST | `` | 新增规则 | `{IndicatorRuleConfig}` |
| PUT | `/{ruleId}` | 更新规则 | `{IndicatorRuleConfig}` |
| DELETE | `/{ruleId}` | 删除规则 | - |
| POST | `/test` | **测试规则** | `{indicatorCode, testValue}` |
| PUT | `/weights` | 更新权重配置 | `{dimensionWeights[]}` |
| GET | `/weights` | 获取当前权重 | - |
| POST | `/import` | 批量导入规则 | FormData: Excel文件 |
| GET | `/export-template` | 下载导入模板 | - |

---

### 6.10 审计日志API

**基础路径**：`/api/audit-logs`

| 方法 | 路径 | 功能 | 查询参数 |
|-----|------|------|---------|
| GET | `` | 日志列表 | `page, size, module, user, dateRange` |
| GET | `/{logId}` | 日志详情 | - |

---

## 7. 核心业务流程

### 7.1 报表采集与处理流程

```
┌─────────────┐
│  客户经理   │
│  打开小程序  │
└──────┬──────┘
       │
       ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  选择采集方式  │───│  拍照/上传   │───│  文件上传    │
│  (拍照/PDF)  │    │  选择文件    │    │  进度显示    │
└─────────────┘    └──────┬──────┘    └──────┬──────┘
                          │                  │
                          ▼                  ▼
                   ┌─────────────┐    ┌─────────────┐
                   │  触发OCR识别  │◄───│  文件存储    │
                   │  (同步/异步) │    │  返回fileId  │
                   └──────┬──────┘    └─────────────┘
                          │
                          ▼
                   ┌─────────────┐
                   │  OCR引擎处理  │
                   │  • PDFBox解析 │
                   │  • 坐标定位   │
                   │  • 字段提取   │
                   │  • 140个字段  │
                   └──────┬──────┘
                          │
                          ▼
                   ┌─────────────┐
                   │  置信度评估  │
                   │  • ≥90%:自动 │
                   │  • 80-90%:关注│
                   │  • <80%:复核 │
                   └──────┬──────┘
                          │
              ┌───────────┴───────────┐
              │                       │
              ▼                       ▼
     ┌─────────────┐         ┌─────────────┐
     │  高置信度    │         │  低置信度    │
     │  自动入库    │         │  进入复核队列  │
     └─────────────┘         └──────┬──────┘
                                     │
                                     ▼
                              ┌─────────────┐
                              │  人工复核    │
                              │  • 字段修正  │
                              │  • 映射调整  │
                              │  • 数值校对  │
                              └──────┬──────┘
                                     │
                                     ▼
                              ┌─────────────┐
                              │  勾稽校验    │
                              │  • 平衡校验  │
                              │  • 逻辑校验  │
                              └──────┬──────┘
                                     │
                                     ▼
                              ┌─────────────┐
                              │  入库完成    │
                              │  • 三表存储  │
                              │  • 指标计算  │
                              │  • 健康度评分  │
                              └─────────────┘
```

### 7.2 健康度评分计算流程

```
┌─────────────────────────────────────────────────────────────┐
│                    健康度评分计算引擎                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  输入：报表ID (reportId)                                      │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Step 1: 加载三大报表数据                               │   │
│  │ • BalanceSheet (65项)                                 │   │
│  │ • IncomeStatement (37项)                               │   │
│  │ • CashFlowStatement (38项)                             │   │
│  └─────────────────────┬───────────────────────────────┘   │
│                        ▼                                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Step 2: 计算30+财务指标 (BigDecimal精度)              │   │
│  │                                                      │   │
│  │ ┌────────────┬────────────┬────────────┐            │   │
│  │ │ 偿债能力    │ 盈利能力    │ 现金流能力  │            │   │
│  │ │ • 流动比率  │ • 毛利率    │ • 现金比率  │            │   │
│  │ │ • 速动比率  │ • 净利率    │ • 现金负债率 │            │   │
│  │ │ • 资产负债率│ • ROE      │ • 现金利息保│            │   │
│  │ │ • ...       │ • ROA      │ │ • ...      │            │   │
│  │ ├────────────┼────────────┼────────────┤            │   │
│  │ │ 运营效率    │ 成长能力    │             │            │   │
│  │ │ • 存货周转  │ • 营收增长  │             │            │   │
│  │ │ • 应收周转  │ • 净利增长  │             │            │   │
│  │ │ • 总资产周转│ • 资产增长  │             │            │   │
│  │ └────────────┴────────────┴────────────┘            │   │
│  └─────────────────────┬───────────────────────────────┘   │
│                        ▼                                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Step 3: 各维度评分 (0-100分)                          │   │
│  │                                                      │   │
│  │ 偿债能力得分 = Σ(指标得分 × 指标权重) / Σ权重         │   │
│  │ 指标得分 = 基于阈值区间映射                            │   │
│  │   • 正常区间 → 80-100分                               │   │
│  │   • 关注区间 → 60-79分                                │   │
│  │   • 高风险区间 → 0-59分                               │   │
│  │                                                      │   │
│  │ 维度得分示例：                                        │   │
│  │   偿债能力 = 85分 (权重30%)                           │   │
│  │   盈利能力 = 72分 (权重25%)                           │   │
│  │   现金流 = 68分 (权重20%)                             │   │
│  │   运营效率 = 75分 (权重15%)                           │   │
│  │   成长潜力 = 65分 (权重10%)                           │   │
│  └─────────────────────┬───────────────────────────────┘   │
│                        ▼                                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Step 4: 加权汇总                                       │   │
│  │                                                      │   │
│  │ TotalScore = 85×0.30 + 72×0.25 + 68×0.20             │   │
│  │            + 75×0.15 + 65×0.10                       │   │
│  │          = 25.5 + 18.0 + 13.6 + 11.25 + 6.5          │   │
│  │          = 74.85 ≈ **74.85分**                        │   │
│  └─────────────────────┬───────────────────────────────┘   │
│                        ▼                                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Step 5: 风险等级判定                                   │   │
│  │                                                      │   │
│  │ 74.85分 → **良好 (GOOD)** 🟢                         │   │
│  │                                                      │   │
│  │ 等级标准：                                            │   │
│  │ • ≥80: 优秀 (EXCELLENT)                               │   │
│  │ • 70-79: 良好 (GOOD)                                  │   │
│  │ • 60-69: 一般 (NORMAL)                                 │   │
│  │ • 50-59: 需关注 (ATTENTION)                            │   │
│  │ • <50:  高风险 (DANGEROUS)                             │   │
│  └─────────────────────┬───────────────────────────────┘   │
│                        ▼                                     │
│  输出：HealthScoreVO {totalScore, dimensions[], riskLevel}   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 7.3 分析报告生成流程

```
┌─────────────────────────────────────────────────────────────┐
│                    分析报告生成引擎                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  触发条件：                                                  │
│  • 手动触发：用户点击"生成报告"按钮                           │
│  • 自动触发：报表入库完成后自动生成                           │
│  • 强制刷新：数据变更后重新生成                               │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 数据准备层                                           │   │
│  │                                                      │   │
│  │ 1. 加载报表详情 (ReportDetailVO)                      │   │
│  │ 2. 计算30+指标 (Map<String, Object>)                  │   │
│  │ 3. 计算健康度 (HealthScoreVO)                         │   │
│  │ 4. 获取历史趋势 (List<TrendVO>)                       │   │
│  │ 5. 获取规则配置 (IndicatorRuleConfig[])              │   │
│  └─────────────────────┬───────────────────────────────┘   │
│                        ▼                                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 内容生成层                                           │   │
│  │                                                      │   │
│  │ generateExecutiveSummary()                           │   │
│  │   → 基于模板 + 健康度分数 + 关键指标 → 生成摘要       │   │
│  │                                                      │   │
│  │ generateOverallAssessment()                          │   │
│  │   → 五维得分分析 + 行业对比 → 综合评价               │   │
│  │                                                      │   │
│  │ generateKeyFindings()                                │   │
│  │   → 筛选异常指标 + 阈值对比 → 关键发现               │   │
│  │                                                      │   │
│  │ generateRiskAnalysis()                               │   │
│  │   → 风险指标排序 + 严重程度评估 → 风险清单           │   │
│  │                                                      │   │
│  │ generatePositiveFactors()                            │   │
│  │   → 优势指标筛选 → 积极因素                           │   │
│  │                                                      │   │
│  │ generateImprovementSuggestions()                     │   │
│  │   → 问题匹配知识库 → 改进建议                         │   │
│  │                                                      │   │
│  │ generateDataQualityNotes()                           │   │
│  │   → 识别准确率 + 完整性检查 → 数据质量说明            │   │
│  └─────────────────────┬───────────────────────────────┘   │
│                        ▼                                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 输出层                                               │   │
│  │                                                      │   │
│  │ • 在线展示：AnalysisReportVO → 前端Markdown渲染      │   │
│  │ • PDF导出：FinancialAnalysisPdfGenerator → PDF文件     │   │
│  │ • JSON接口：RESTful API → 小程序/第三方集成           │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 8. 数据模型设计

### 8.1 核心实体关系图（简化）

```
┌──────────────┐       ┌──────────────────┐       ┌──────────────┐
│   Enterprise │──────<│FinancialReportArchive│──────>│  OcrTask    │
│   (企业)     │ 1    N │   (报表归档)       │ N    1 │  (OCR任务)  │
└──────────────┘       ├──────────────────┤       └──────┬──────┘
                       │                  │              │
                       │    ┌─────────────┼──────────────┐│
                       │    │             │              ││
                       │    ▼             ▼              ▼│
                       │ ┌──────────┐ ┌──────────┐ ┌──────────┐
                       │ │BalanceSheet│ │IncomeStmt │ │CashFlow  │
                       │ │(资产负债表)│ │ (利润表)  │ │(现金流量)│
                       │ └─────┬────┘ └─────┬────┘ └─────┬────┘
                       │       │            │            │
                       │       ▼            ▼            ▼
                       │  ┌─────────────────────────────┐
                       │  │    FinancialIndicatorValue    │
                       │  │      (财务指标值)             │
                       │  └──────────────┬──────────────┘
                       │                 │
                       │                 ▼
                       │  ┌────────────────────────────┐
                       │  │   FinancialHealthScore      │
                       │  │     (健康度评分)            │
                       │  └──────────────┬─────────────┘
                       │                 │
                       │                 ▼
                       │  ┌────────────────────────────┐
                       │  │  FinancialAnalysisReport    │
                       │  │      (分析报告)             │
                       │  └────────────────────────────┘
                       │
                       │    ┌──────────────────┐
                       │    │OcrFieldResult    │
                       │    │ (OCR识别结果)    │
                       │    └──────────────────┘
                       └──────────────────────────────────
```

### 8.2 核心数据表清单（19张表）

| 序号 | 表名 | 实体类 | 用途 | 记录数估算 |
|-----|------|-------|------|-----------|
| 1 | sys_user | User | 用户账户 | 100-1000 |
| 2 | enterprise | Enterprise | 企业档案 | 100-10000 |
| 3 | financial_report_archive | FinancialReportArchive | 报表归档 | 1000-100000 |
| 4 | balance_sheet | BalanceSheet | 资产负债表 | 同上 |
| 5 | balance_sheet_item | BalanceSheetItem | 资产负债表明细 | ×65 |
| 6 | income_statement | IncomeStatement | 利润表 | 同上 |
| 7 | income_statement_item | IncomeStatementItem | 利润表明细 | ×37 |
| 8 | cash_flow_statement | CashFlowStatement | 现金流量表 | 同上 |
| 9 | cash_flow_statement_item | CashFlowStatementItem | 现金流量表明细 | ×38 |
| 10 | ocr_task | OcrTask | OCR任务 | 同上 |
| 11 | ocr_field_result | OcrFieldResult | OCR字段结果 | ×140 |
| 12 | uploaded_file | UploadedFile | 上传文件 | 同上 |
| 13 | financial_indicator | FinancialIndicator | 指标定义 | 33 |
| 14 | financial_indicator_value | FinancialIndicatorValue | 指标计算值 | 大量 |
| 15 | financial_health_score | FinancialHealthScore | 健康度评分 | 同上 |
| 16 | historical_indicator_value | HistoricalIndicatorValue | 历史指标值 | 大量 |
| 17 | financial_analysis_report | FinancialAnalysisReport | 分析报告 | 同上 |
| 18 | indicator_rule_config | IndicatorRuleConfig | 规则配置 | 33+ |
| 19 | audit_log | AuditLog | 审计日志 | 海量 |

---

## 9. 非功能性需求

### 9.1 性能需求

| 指标项 | 要求 | 说明 |
|-------|------|------|
| **页面加载时间** | ≤2秒 | 首屏渲染完成（4G网络） |
| **API响应时间** | ≤500ms | 单接口P99响应时间 |
| **OCR识别速度** | 图片≤10秒，PDF≤30秒 | 标准格式报表 |
| **并发用户数** | ≥100 | 同时在线用户 |
| **数据处理量** | 支持10万+企业 | 单实例部署 |

### 9.2 安全需求

| 安全项 | 要求 | 实现方式 |
|-------|------|---------|
| **传输加密** | HTTPS强制 | SSL/TLS证书 |
| **认证机制** | JWT Token | 2小时有效期 + 刷新机制 |
| **权限控制** | RBAC角色权限 | 管理员/分析师/操作员 |
| **数据脱敏** | 日志敏感信息掩码 | 手机号/身份证部分隐藏 |
| **防重放攻击** | 请求唯一标识 | Nonce + Timestamp |
| **SQL注入防护** | 参数化查询 | JPA/Hibernate ORM |
| **XSS防护** | 输入转义 | Vue自动转义 |

### 9.3 可用性需求

| 指标 | 要求 |
|------|------|
| **系统可用性** | ≥99.5%（年度） |
| **故障恢复时间** | ≤30分钟（RTO） |
| **数据备份频率** | 每日全量 + 实时增量 |
| **灾难恢复点** | ≤1小时数据丢失（RPO） |

### 9.4 兼容性需求

| 项目 | 要求 |
|------|------|
| **微信小程序** | 基础库 ≥2.20.0，iOS/Android双端 |
| **管理后台浏览器** | Chrome ≥90, Edge ≥90, Firefox ≥88 |
| **屏幕分辨率** | 最小支持1366×768，推荐1920×1080 |
| **服务器OS** | CentOS 7+/Ubuntu 20.04+ |
| **JDK版本** | JDK 1.8 (必须) |
| **Node.js版本** | ≥14.x（推荐16.x/18.x） |

---

## 10. 附录

### 附录A：术语表

| 术语 | 全称 | 定义 |
|------|------|------|
| OCR | Optical Character Recognition | 光学字符识别 |
| KPI | Key Performance Indicator | 关键绩效指标 |
| ROE | Return on Equity | 净资产收益率 |
| ROA | Return on Assets | 总资产收益率 |
| PRD | Product Requirements Document | 产品需求文档 |
| API | Application Programming Interface | 应用程序接口 |
| CRUD | Create/Read/Update/Delete | 增删改查 |
| JWT | JSON Web Token | JSON网络令牌 |
| RBAC | Role-Based Access Control | 基于角色的访问控制 |
| ECharts | Enterprise Charts | 百度开源可视化库 |

### 附录B：参考文档

- 产品原型文件：`originfile/` 目录下的原型设计稿
- 技术架构文档：`.trae/documents/TechnicalArchitecture-*.md`
- 数据库设计文档：`docs/database-design.md`
- API接口文档：`docs/api.md`
- 部署运维手册：`DEPLOYMENT.md`
- 交付验收报告：`DELIVERY_REPORT.md`

### 附录C：修订历史

| 版本 | 日期 | 修订内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2026-07-28 | 初版创建 | System |
| v2.0 | 2026-08-05 | **基于实际代码重构，完善功能细节和API定义** | System |

---

## 文档结束

**文档维护**：本文档由产品团队和技术团队共同维护，如有疑问请联系项目负责人。

**版权声明**：© 2026 鑫速录团队. All Rights Reserved.
