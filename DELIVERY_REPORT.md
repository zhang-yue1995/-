# 鑫速录交付验收报告

最近验收日期：2026-08-03

## 已完成

- 按管理后台原型重构登录、工作台、企业与报表、报表详情、财务分析、历史趋势和规则配置。
- 补齐 OCR 任务中心、审计日志、系统状态三个原有侧栏入口，消除无路由菜单。
- 按报表原件还原资产负债表、利润表、现金流量表的项目、金额、期间和单位。
- 接通小程序上传、OCR、建档、复核、三张报表、校验、健康度、分析报告和趋势接口。
- 文件上传改为真实落盘与下载，限制 30 MB，使用 UUID 存储名并阻止路径穿越。
- API 除登录与健康检查外统一要求 Bearer Token；密码、Token 有效期、CORS 来源均可由环境变量配置。
- OCR 支持 `mock` 验收模式和标准化 HTTP 生产适配器；生产未配置真实服务时明确失败，不伪装识别成功。
- 指标规则与五维权重改为数据库持久化，并直接参与健康评分。
- 关键写操作写入审计日志。
- 增加 Docker、Nginx、数据卷、环境变量模板、健康检查和备份恢复说明。
- 增加标准三表 65/37/38 字段模板；即使金额为空或识别失败，也保留待人工复核的完整字段行。
- 对带文字层 PDF 本地提取企业名称、报表期、单位和本期金额，并将报表期、客户经理贯通到管理后台列表。
- 小程序复核入库后增加完成页、“结束并返回首页”和“查看已入库报表”操作。

## 报表原件基准

企业：江苏曼斯特机电科技有限公司  
期间：2026-03  
单位：元

| 项目 | 验收值 |
|---|---:|
| 资产总计 | 7,923,544.71 |
| 负债合计 | 7,995,201.81 |
| 所有者权益 | -71,657.10 |
| 营业收入 | 773,769.57 |
| 营业成本 | 447,446.55 |
| 营业利润 | -10,446.59 |
| 净利润 | -9,950.59 |
| 经营活动现金流净额 | -391,006.53 |
| 期末现金 | 1,034,959.57 |

健康度五维得分：`29.50 / 31.75 / 9.00 / 9.00 / 22.67`。  
加权总分：`22`。风险等级：`DANGEROUS`。

## 自动化验证结果

| 验证项 | 结果 |
|---|---|
| 后端 Maven 集成测试 | 通过 |
| 真实三页 PDF 解析回归测试 | 140 字段、表头及关键金额通过 |
| 上传→下载→OCR→建档→复核→三张报表 | 通过 |
| 35 项指标与健康度幂等计算 | 通过 |
| API 鉴权 | 未授权请求 HTTP 401 |
| 健康检查 | HTTP 200，`status=UP` |
| 管理后台生产构建 | 通过 |
| 小程序 JS 语法 | 33/33 通过 |
| 小程序 JSON 解析 | 24/24 通过 |
| 小程序 Tab 图标 | 8/8 存在且有效 |
| 浏览器登录/工作台/报表/分析/趋势/规则 | 通过 |
| 浏览器 OCR中心/审计日志/系统状态 | 通过 |
| ECharts 运行渲染 | 分析页 1 个画布，趋势页 6 个画布 |
| 规则权重持久化 | 5 个维度，权重合计 100 |
| 运行日志 | 0 个 ERROR/编译错误 |

生产依赖安全审计结果：高危 `0`、中危 `0`、低危 `3`。低危项来自 Vue 2 及其绑定的 Element UI/Vuex 技术栈；跨代升级会改变现有原型组件行为，未在本次还原交付中强行迁移。Axios 与 ECharts 已升级到审计建议的修复版本。

## 构建产物

- 后端：`backend/target/xinsulu-backend-1.0.0.jar`
- 管理后台：`frontend/dist`
- 小程序：`wxapp`
- 容器部署：`docker-compose.yml`
- 环境模板：`.env.example`
- 部署说明：`DEPLOYMENT.md`

## 上线前置条件

1. 复制 `.env.example` 为 `.env` 并修改两个默认密码。
2. 生产设置 `DEMO_DATA_ENABLED=false`。
3. 若需要处理扫描件或图片，生产设置 `AI_PROVIDER=http`，配置真实 `OCR_HTTP_ENDPOINT` 和 Token；带文字层的标准 PDF 可直接本地解析。
4. 配置管理后台 HTTPS 域名及 `PUBLIC_ORIGIN`。
5. 在 `wxapp/config.js` 填写正式 HTTPS API 地址，并在微信公众平台登记合法域名。
6. 先执行数据卷备份演练，再导入真实客户数据。

本机未安装 Docker CLI，因此无法在本机实际拉取镜像执行 `docker compose up`；Compose YAML 已通过解析，前后端已分别完成生产构建与运行时联调。部署机上仍需按 `DEPLOYMENT.md` 执行一次容器启动验收。
